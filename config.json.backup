{
  "QBitTorrent": {
    "url": "127.0.0.1:8081",
    "username": "",
    "password": "",

    //qbittorrent.exe路径,若QB没有运行则会通过此路径运行,留空则不自动运行
    "path": "C:\\Program Files\\qBittorrent\\qbittorrent.exe",

    //不检查tracker、不辅种的目录
    "ignore": [""]
  },
  //辅种功能
  "ReSeed": {
    "switch": false,

    //IYUU辅种接口 申请地址https://iyuu.cn/
    "token":""
  },
  "BYR": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "BYR",
      "TJU",
      "PTER",
      "MTeam",
      "FRDS",
      "PTHOME"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "TJU": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "TJU",
      "BYR",
      "PTER",
      "MTeam",
      "FRDS",
      "PTHOME"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168

  },
  "PTER": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "PTER",
      "TJU",
      "BYR",
      "MTeam",
      "FRDS",
      "PTHOME"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "MTEAM": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "MTeam",
      "PTER",
      "TJU",
      "BYR",
      "FRDS",
      "PTHOME"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "PTHOME": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "PTHOME",
      "PTER",
      "TJU",
      "BYR",
      "MTeam",
      "FRDS"
    ],
    "KeepTorrentTime": 168
    //种子多久时间内不会被删除,自添加开始计算,默认一星期,单位:小时
  },
  "FRDS": {
    /总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    //用于上传限速, 0表示不限速, 单位Mb/s。
    //Ultimate User以下 上传限速100Mb/s, Ultimate User 上传限速200Mb/s, Nexus Master及以上不限速
    "UploadSpeedLimit": 100,

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "FRDS",
      "PTHOME",
      "PTER",
      "TJU",
      "BYR",
      "MTeam"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "TTG": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "TTG",
      "PTHOME",
      "PTER",
      "TJU",
      "BYR",
      "MTeam"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "LEMONHD": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "LEMONHD",
      "PTHOME",
      "PTER",
      "TJU",
      "BYR",
      "MTeam"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },
  "SoulVoice": {
    //总开关
    "switch": false,
    //仅签到模式
    "onlyAttendance": false,
    //扫描站点种子时间间隔,单位：分钟 默认30分钟
    "IntervalTime": 30,
    //站点passkey，用于辅种下种
    "passkey": "",

    // 此站点总种子大小容量,容量不够时会自动删种 默认0且不开启磁盘容量管理,-1为根据磁盘剩余容量动态变化
    // 单位：TB
    "CapacityNum": -1,

    //种子会保存在第一个分类,如需要接管其他分类,在后面追加即可。 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下(即在同一个磁盘分区),否则不接管
    "MainCategory": [
      "SoulVoice",
      "PTHOME",
      "PTER",
      "TJU",
      "BYR",
      "MTeam"
    ],

    //保留种子时间(不会被删除),默认一星期,单位:小时
    "KeepTorrentTime": 168
  },

  "log": {
    "LogLevel": "info",
    //日志等级,可选info,debug 默认info
    "LogSaveTime": 7
    //日志最长保存时间,单位：天 默认7天
  }
}
