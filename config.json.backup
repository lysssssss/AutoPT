{
  "QBitTorrent": {
    "url": "127.0.0.1:8081",
    "path": "C:\\Program Files\\qBittorrent\\qbittorrent.exe"
    //qbittorrent.exe路径,若QB没有运行则会通过此路径运行,留空则不自动运行
  },
  "log": {
    "LogLevel": "info",
    //日志等级,可选info,debug 默认info
    "LogSaveTime": 5
    //日志最长保存时间,单位：天 默认7天
  },
  "ReSeed": {
    "switch": false,
    "token":""
    //IYUU辅种接口 申请地址https://iyuu.cn/
  },
  "BYR": {
    "switch": false,
    //总开关
    "IntervalTime": 10,
    //检查PT站的时间间隔,单位：分钟 默认10分钟
    "passkey": "",
    //站点passkey，用于辅种功能
    "CapacityNum": -1,
    // 下载目录最大容量,容量不够时会自动删种 默认0且不开启磁盘管理,-1为根据磁盘剩余容量动态变化
    "CapacityUint": "gb",
    //容量单位,只能GB或TB,不区分大小写,始终以字节为单位 默认GB

    //自动下载的种子会自动归类到第一个分类下,如需要接管其他分类,在数组里追加即可 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下,否则不接管
    "MainCategory": [
      "BYR",
      "TJU",
      "PTER",
      "MTeam"
    ],
    "KeepTorrentTime": 24
    //种子多久时间内不会被删除,自添加开始计算,默认0,单位:小时

  },
  "TJU": {
    "switch": false,
    //总开关
    "IntervalTime": 10,
    //检查PT站的时间间隔,单位：分钟 默认10分钟
    "passkey": "",
    //站点passkey，用于辅种功能
    "CapacityNum": -1,
    // 下载目录最大容量,容量不够时会自动删种 默认0且不开启磁盘管理,-1为根据磁盘剩余容量动态变化
    "CapacityUint": "gb",
    //容量单位,只能GB或TB,不区分大小写,始终以字节为单位 默认GB

    //自动下载的种子会自动归类到第一个分类下,如需要接管其他分类,在数组里追加即可 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下,否则不接管
    "MainCategory": [
      "TJU",
      "BYR",
      "PTER",
      "MTeam"
    ],
    "KeepTorrentTime": 24
    //种子多久时间内不会被删除,自添加开始计算,默认0,单位:小时

  },
  "PTER": {
    "switch": false,
    //总开关
    "IntervalTime": 10,
    //检查PT站的时间间隔,单位：分钟 默认10分钟
    "passkey": "",
    //站点passkey，用于辅种功能
    "CapacityNum": -1,
    // 下载目录最大容量,容量不够时会自动删种 默认0且不开启磁盘管理,-1为根据磁盘剩余容量动态变化
    "CapacityUint": "gb",
    //容量单位,只能GB或TB,不区分大小写,始终以字节为单位 默认GB

    //自动下载的种子会自动归类到第一个分类下,如需要接管其他分类,在数组里追加即可 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下,否则不接管
    "MainCategory": [
      "PTER",
      "TJU",
      "BYR",
      "MTeam"
    ],
    "KeepTorrentTime": 24
    //种子多久时间内不会被删除,自添加开始计算,默认0,单位:小时
  },
  "MTEAM": {
    "switch": false,
    //总开关
    "IntervalTime": 10,
    //检查PT站的时间间隔,单位：分钟 默认10分钟
    "passkey": "",
    //站点passkey，用于辅种功能
    "CapacityNum": -1,
    // 下载目录最大容量,容量不够时会自动删种 默认0且不开启磁盘管理,-1为根据磁盘剩余容量动态变化
    "CapacityUint": "gb",
    //容量单位,只能GB或TB,不区分大小写,始终以字节为单位 默认GB

    //自动下载的种子会自动归类到第一个分类下,如需要接管其他分类,在数组里追加即可 默认未分类
    //接管的分类必须与第一个分类处于同一个分区下,否则不接管
    "MainCategory": [
      "MTeam",
      "PTER",
      "TJU",
      "BYR"
    ],
    "KeepTorrentTime": 24
    //种子多久时间内不会被删除,自添加开始计算,默认0,单位:小时
  }
}